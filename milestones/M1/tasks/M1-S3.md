# M1-S3 — Exact-hash Public Workflow Evidence

- Task ID: `M1-S3`
- Milestone: `M1`
- State: `ready`
- Route: `OPUS`
- Task owner: Claude Code Opus execution lead
- Final reviewer: Codex

## Objective

Validate the user-reviewed M1 candidate through the documented public operator
workflow on its exact current disk hash. Produce fresh runtime, capture, visual,
cleanup, and immutability evidence without editing or saving the candidate.

This replaces the obsolete instruction to resume Claude session
`7341215d-361d-46c6-a040-fa86d39ff670`. Start from current files and runtime
facts; do not inherit that session's permission or wrapper-error conclusions.

## Required inputs

- `PROJECT_PROFILE.md`
- `scripts/demo_start.py`
- `scripts/ik_controller.py`
- `scripts/capture_d455.py`
- `validation/run_baseline_acceptance.py` as historical validation guidance only
- `validation/validate_m1_step1.py`
- `validation/validate_m1_step2.py`
- `config/object_catalog.yaml`
- `milestones/M1/STATUS.md`
- `milestones/M1/LOG.md`

## Authoritative facts

- Protected baseline:
  `scenes/ridgeback_franka_d455_demo.usd`
- Protected baseline SHA-256:
  `a724cd7da8c31ced82cba32a41c4abdf75d8011e4baebf274079c30e2c44a7cc`
- Candidate:
  `scenes/ridgeback_franka_d455_yolo_demo.usd`
- Expected candidate SHA-256:
  `05329ddde64616b1bc05287520002e3c862942572225006232bfa76ee0b01758`
- Factory content layer SHA-256:
  `4cb30f7854f486bd77f83b5bd47530ce2d920f006d82bcbb82f3047b69a5ff49`
- Capture `2026-0731-5` is framing reference only. Its recorded scene hash is
  not the expected candidate hash and it cannot satisfy this task.

## Allowed actions

- Read project files and hashes.
- Use the registered `isaac-sim` MCP server.
- Cleanly reopen the exact candidate after confirming that doing so will not
  discard an uncommitted live USD state.
- Run Play and the public `scripts/demo_start.py` entry point.
- Observe IKTarget, Panda joints, panda_hand, D455, callbacks, RGB, and depth.
- Call the public capture and stop functions.
- Create ignored scratch evidence under `validation/tmp/` and one unique capture
  directory under `outputs/captures/`.
- Append only `milestones/M1/LOG.md` among tracked coordination files.
- Opus may delegate bounded read-only or runtime-validation subtasks to Sonnet,
  but must integrate and review their output.

## Forbidden actions

- Do not edit, save, replace, or re-export either USD.
- Do not move or rotate `/World/IKTarget` for this task.
- Do not modify factory layout, catalog, assets, scripts, MCP registration,
  credentials, or server implementation.
- Do not cite stale scratch files or Capture 5 as exact-hash acceptance.
- Do not modify `STATUS.md` or `CURRENT.md`.
- Do not commit, push, enter M2, or declare M1 accepted.

## Execution requirements

1. Record initial Git status and all authoritative disk hashes.
2. Complete the three-channel Isaac preflight and verify required prims.
3. Cleanly open the exact candidate without saving another live stage.
4. Record pre-Play Panda joint positions, IKTarget pose, panda_hand pose, D455
   camera pose, timeline, and root dirty state.
5. Press Play and execute the public `scripts/demo_start.py` bootstrap exactly as
   an operator would. Do not load private controller/capture scripts as a
   substitute for this gate.
6. Keep IKTarget unchanged. After settling, record IKTarget, Panda joints,
   panda_hand, camera pose, subscription count, controller error count, and the
   startup deltas.
7. Call the public unlabeled `demo_capture()` once. Record its unique directory
   and exact scene hash.
8. Call the public `demo_stop()`, stop the timeline, and verify callback cleanup.
9. Confirm both USD disk hashes are unchanged.
10. Inspect the new RGB image and depth preview directly. Confirm the intended
    full rack and representative contents are useful in the frame. Inspect raw
    depth validity and stereo baseline numerically.
11. Write `validation/tmp/claude_m1_s3_result.json` using a fresh timestamp and
    append the required M1 log entry.

## Required evidence

- Active stage path and exact candidate hash.
- Public `demo_start.py` result.
- IKTarget translation/orientation delta across startup.
- Panda seven-joint startup delta and any visible snap assessment.
- panda_hand and left-camera startup poses.
- Exactly one live IK subscription and controller error count zero.
- Unique capture directory whose capture metadata contains the exact candidate
  hash before and after.
- Non-black RGB, valid raw depth, and stereo baseline `0.095 m ± 0.002 m`.
- Direct visual review of RGB and depth preview.
- IKTarget visibility restoration.
- `demo_stop()` result, subscription count zero, and stopped timeline.
- Protected and candidate USD hashes unchanged after the run.
- Failed probes, retries, limitations, and any Sonnet delegation.

## Stop conditions

Stop without mutation when:

- any authoritative disk hash is wrong;
- active stage cannot be safely changed to the exact candidate;
- a clean reopen would discard an uncommitted live USD state;
- the public workflow would require moving IKTarget or saving the USD;
- protected baseline changes;
- MCP cannot execute and no fresh scratch envelope proves completion;
- the capture metadata does not reference the exact candidate hash.

## Handoff to Codex

Report `completed`, `needs_review`, `blocked`, or `failed`; list evidence and
capture paths; append `milestones/M1/LOG.md`; and stop. Codex will independently
inspect runtime state, images, hashes, diff, and evidence, discuss any remaining
gap with the user, and decide follow-up or acceptance by following
`docs/CODEX_M1_S3_REVIEW_PROMPT.md`.
